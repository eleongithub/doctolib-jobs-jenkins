---
# tasks file for jdk
- name: Get JDK/Java version installed on the node
  shell: java -version 2>&1
  register: java_version_result
  ignore_errors: True

- include: install.yml
  when: ((java_version_result|failed) or (java_version_result.stdout.find('{{ jdk_java_version }}') != -1))